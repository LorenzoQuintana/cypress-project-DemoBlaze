{
  "name": "demoblaze-cucumber",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run": "cypress run",
    "cy:run:regression": "cypress run --env grepTags=@regression",
    "cy:run:smoke": "cypress run --env grepTags=@smoke"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.0.6",
    "cypress": "^13.11.0"
  },
  "dependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.1",
    "esbuild": "^0.21.5",
    "cypress": "^13.11.0",
    "@badeball/cypress-cucumber-preprocessor": "^10.4.0",
    "mocha-junit-reporter": "^2.0.0"
  }
}
